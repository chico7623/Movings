# Railway frontend service
# IMPORTANTE: variáveis VITE_* ficam públicas no bundle do browser.
# Aqui só deve estar a URL pública da API, nunca passwords/secrets.
VITE_PHP_API_URL=https://TEU-BACKEND.up.railway.app
VITE_NODE_API_URL=
